class DurationConst {
  const DurationConst._();

  static const screenTransition = Duration(milliseconds: 350);

  static const iosDefault = Duration(milliseconds: 350);

  static const iosFast = Duration(milliseconds: 100);

  static const dummyLoadData = Duration(milliseconds: 1000);
}
